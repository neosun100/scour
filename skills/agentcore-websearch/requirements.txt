# Runtime dependency for the search CLI (agentcore_websearch.py).
# mcp-proxy-for-aws is AWS's MCP proxy: it SigV4-signs requests and speaks MCP to
# the gateway, so this project doesn't hand-roll signing/transport. It pulls in
# boto3, botocore, and fastmcp. The CLI uses fastmcp's client to drive the proxy.
# Infrastructure creation (setup.sh) uses the AWS CLI, not these.
mcp-proxy-for-aws>=1.6
