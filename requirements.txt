# Runtime dependency for the search CLI (agentcore_websearch.py).
# Only used to resolve credentials and SigV4-sign MCP requests — any recent
# boto3 works (botocore comes transitively). Infrastructure creation uses the
# AWS CLI, not boto3.
boto3>=1.34
